Dialogue portrait animation data.

An array of arrays of frames, each with an `index` and a `duration`.

The `index` determines which `FaceX.png` sprite-sheet to use, and the
`duration` determines for how many frames the sprite-sheet is used.

Each array of frames has an associated numerical ID. This ID is what is used by
the `<FAC` TSC command to select that particular animation.
